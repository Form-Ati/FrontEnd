# 프론트엔드 환경변수 예시 — 복사해서 .env / .env.local 로 채운다.
#
# API 베이스 URL. 비워두면 '/api'(개발은 Vite 프록시가 백엔드로 전달).
# 프로덕션 배포 시 백엔드 도메인을 넣는다. 예) https://168.107.12.171.nip.io/api
# VITE_API_BASE_URL=

# 개발 Vite 프록시가 전달할 백엔드 주소(기본 http://localhost:8080).
# VITE_DEV_API_TARGET=http://localhost:8080
