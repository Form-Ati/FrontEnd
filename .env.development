# 개발 기본값 — VITE_API_BASE_URL 미설정 시 api 클라이언트는 '/api' 를 쓰고
# Vite dev 서버 프록시가 http://localhost:8080 으로 전달한다(CORS 불필요).
# 백엔드를 다른 주소에서 띄우면 VITE_DEV_API_TARGET 으로 바꾼다.
VITE_DEV_API_TARGET=http://localhost:8080
